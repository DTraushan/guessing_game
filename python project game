import random
def game():
    while True:
        try:
            n=int(input("Enter LAST number (0 TO _): "))
            if n<=0:
                print("Enter Number Atleast 1")
                continue
            break
        except ValueError:
            print("Enter Valid Number")
    number=random.randint(0,n)
    attment=0
    while True:
        difficulty=input("Choose difficulty: easy : medium : hard :").casefold()
        if difficulty=="easy":
            max_attment=10
            break
        elif difficulty=="medium":
            max_attment=5
            break
        elif difficulty=="hard":
            max_attment=3
            break
        else:
            print("invalid difficulty")
    while True:
            try:
                guess = int(input("Enter Number: "))
                if not 0<=guess<=n:
                    print(f"Please ENTER NUMBER From 0 to {n}")
                    continue
                difference = abs(guess - number)
                guess_diff=(difference/n)*100
                attment+=1
                if guess>number:
                    print("Go Low")
                elif guess<number:
                    print("Go High")
                else:
                    print("YOU WON")
                    print("Number Of Attment: ",attment)
                    break
                if attment>=max_attment:
                    print("YOU LOST")
                    print("Number Of Attment: ",attment)
                    print("Number Is:",number)
                    break
                if guess_diff<=5:
                    print("🔥 VERY CLOSE")
                elif guess_diff<=10:
                    print(" 🙂 CLOSE")
                else:
                    print(" 🥶 FAR")
                print("Attempts left:", max_attment-attment)

                
            except ValueError:
                print("Please enter Valid a number!")
game()
while True:
    again = input("Play again? yes/no: ").lower()
    if again!="yes":
        break
    game()


